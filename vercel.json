{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "server/app.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/app.ts"
    }
  ]
}
